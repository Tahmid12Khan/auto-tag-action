# This is a basic workflow to help you get started with Actions
name: Auto Tagging
description: 'Creates new tag with prefix "RC-". For example: RC-0.0.1'
author: 'Tahmid Khan'
runs:
  using: "composite"
  steps:
    -run: echo "$(git fetch --tags)"
    shell: bash
